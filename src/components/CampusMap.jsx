import React, { useState, useRef, useEffect } from 'react';
import { Search, ZoomIn, ZoomOut, RotateCcw, MapPin, Info } from 'lucide-react';
import { useBlocks, useRooms } from '../hooks/useApi';
import CollegeMap from './CollegeMap';

const CampusMap = () => {
 
  return (
   
               
                
                  
                 
                   <CollegeMap />
               

                 
              
             
  );
};

export default CampusMap;
